{% extends 'TrackmeBackendBundle::base_admin_assetic_less.html.twig' %}

{% block title %}
Track Me | Payments
{% endblock title %}

{% block navbar %}
{% include('TrackmeBackendBundle::base_admin_navbar.html.twig') %}
{% endblock navbar %}

{% block body %}
<div class="row-fluid">
	<div class="span8">
		<h3>Pagos Pendientes</h3>
		<table class="table table-striped table-hover table-condensed">
			{% for p in payments%}
            <tr class="list_trow">
				<td>Fecha: {{ p.getCreatedAt|date('d/m/Y') }}</td>
				<td>Monto (CLP): ${{ p.getClpAmount }} </td>
                <td>Monto (USD): ${{ p.getAmount }} </td>
                <td><a href="{{ path('admin_business_details_payment', { 'id': p.id})}}">Pagar</a></td>
			</tr>
            {% else %}
            <tr class="list_trow">
                <td>¡Hurra! No hay pagos pendientes</td>
			</tr>
            {% endfor %}
            
		</table>
	</div>
</div>
<div class="row-fluid">
	<div class="span8">
		<h3>Pagos Realizados</h3>
		<table class="table table-striped table-hover table-condensed">
			{% for a in approved%}
            <tr class="list_trow">
				<td>Fecha: {{ a.getCreatedAt|date('d/m/Y') }}</td>
				<td>Monto: ${{ a.getAmount }} </td>
			</tr>
            {% else %}
            <tr class="list_trow">
                <td>¡Hurra! Por el momento no hay pagos aprovados</td>
			</tr>
            {% endfor %}
            
		</table>
	</div>
</div>

{% endblock %}

{% block footer %}
<footer>
	<hr>
	<p>
		{% block footer_text %}
		&copy; {{"now"|date("Y")}} Track Me! Todos los derechos reservados
		{% endblock %}
	</p>
</footer>
{% endblock footer %}
